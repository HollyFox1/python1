#Set user entry and variables
user_word = str(input("Enter a word:"))
word_without_vowels = ""
vowels = "AEIOU" #this either needs to be a list, or a string

#Loop to check if each letter in user_word matches vowles
#If it matches continue and do nothing with it
#If it does not match then add the letter to word_without_vowels

for letter in user_word:
    if letter.upper() in vowels:     
        print()
    else:
        word_without_vowels = word_without_vowels + letter

#This prints the variable word_without_vowels
print(word_without_vowels)
